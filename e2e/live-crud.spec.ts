/**
 * ─── Live Site CRUD Tests (Disabled) ───────────────────────────
 * Disabled to comply with the constraint:
 * "local only on all tests remember the production db and server should not be touched
 * cause this is a live actual database only the local db should be tested development server"
 */
import { test } from '@playwright/test'

test.describe.skip('CoopERP Live Site — Full CRUD (Disabled)', () => {
  // All tests disabled
})
